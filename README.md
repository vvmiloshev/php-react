# php-react
PHP и ReactJS

# Project Setup

## Requirements

- Docker
- Docker Compose

## Start the project

Run the following command from the project root:

```bash
docker compose -p php-react up -d --build
